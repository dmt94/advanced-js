/*
Hash Tables = 
*/

let basket = {
  grapes: 1000,
}

console.log(basket.grapes);

//idempotent = a function will always return same output

//Maps maintain order of insertion
const a = new Map()
const b = new Sets() //stores keys no vals
